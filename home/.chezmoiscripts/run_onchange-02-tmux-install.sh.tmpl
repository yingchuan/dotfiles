{{ $sudo := "sudo  " -}}
{{ if eq .chezmoi.username "root" -}}
{{     $sudo = "" -}}
{{ end -}}
#!/bin/bash

{{- if eq .chezmoi.os "linux" }}
{{ $sudo }} apt install -yy tmux
{{ else if .chezmoi.os "darwin" }}
HOMEBREW_NO_INSTALL_CLEANUP=true brew install tmux
{{ end -}}

if [ ! -d ~/.tmux ]; then
  ln -sf ~/.tmux/.tmux.conf ~/
fi
